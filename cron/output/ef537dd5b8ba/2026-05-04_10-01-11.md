# Cron Job: Hermes 每周更新检查

**Job ID:** ef537dd5b8ba
**Run Time:** 2026-05-04 10:01:11
**Schedule:** 0 10 * * 1

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

检查 Hermes Agent 是否有更新：

```bash
cd ~/.hermes/hermes-agent && git fetch origin 2>/dev/null
CURRENT=$(git rev-parse HEAD)
REMOTE=$(git rev-parse origin/main)
if [ "$CURRENT" != "$REMOTE" ]; then
  UPDATES=$(git log --oneline HEAD..origin/main)
  echo "有更新可用！当前版本落后于 origin/main"
  echo "待更新 commit:"
  echo "$UPDATES"
  echo ""
  echo "最新版本信息:"
  git log origin/main --oneline -5
else
  echo "Hermes 已是最新版本，无需更新。"
fi
```

注意：
- 不要自动更新（hermes update 会重启 gateway），只检查并报告
- 如果有更新，在消息中告诉用户可以输入 /update 进行更新
- 发送格式：标题 + 更新内容列表 + 最新5条commit


## Response

## 🚀 Hermes Agent 更新通知

**有更新可用！** 当前版本落后于 `origin/main`，共有 **200+ 个 commit** 待更新。

### 待更新 commit（部分）：

| # | Commit | 说明 |
|---|--------|------|
| 1 | `363cc93` | fix(cron): bump skill usage when cron jobs load skills |
| 2 | `808fee1` | fix(auxiliary): propagate explicit_api_key to _try_anthropic() |
| 3 | `74636f9` | fix(gateway): clear queued reload-skills notes on new/resume/branch |
| 4 | `222767e` | fix: sanitize Telegram help command mentions |
| 5 | `6fda92a` | fix(gateway): bridge top-level require_mention to Telegram config |
| 6 | `1bd975c` | fix(gateway): suppress duplicate voice transcripts |
| 7 | `b58db23` | fix(kanban): drop worker identity claim from KANBAN_GUIDANCE |
| 8 | `6713274` | fix(file): strip leaked terminal fences from reads |
| 9 | `2d7543c` | fix(windows): enforce UTF-8 stdout/stderr to prevent UnicodeEncodeError crash |
| 10 | `2ababfe` | chore(release): map 0xKingBack noreply email |
| ... | *(共 200+ 个)* | |

### 最新 5 条 commit：

```
363cc937 fix(cron): bump skill usage when cron jobs load skills
808fee151 fix(auxiliary): propagate explicit_api_key to _try_anthropic()
74636f9c4 fix(gateway): clear queued reload-skills notes on new/resume/branch
222767e5e fix: sanitize Telegram help command mentions
6fda92aa7 fix(gateway): bridge top-level require_mention to Telegram config
```

---

💡 **提示：** 输入 `/update` 即可自动更新 Hermes Agent（更新过程中 gateway 会重启）。
